# ui
## Start here

**[`docs/HANDOFF.md`](docs/HANDOFF.md)** — links every document, names the gates, and lists the
traps already paid for.

Trading cockpit UI, reproduced against a competitor teardown. **UI only** — no LLM, no Pine
compilation, no market data, no backtest engine. Everything renders from one seeded fixture.

```bash
npm i && npm run dev   # http://localhost:5199
```
